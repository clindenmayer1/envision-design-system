import { Meta } from '@storybook/blocks';

<Meta title="Foundations/Elevation" />

# Elevation

Seven published roles. Each is a real shadow from the product, reconciled during the audit rather
than generated from a formula, which is why the ramp is not evenly spaced, because a menu and a tray sit
at genuinely different depths in this product.

<div style={{ display: 'grid', gap: '32px', gridTemplateColumns: 'repeat(auto-fill, minmax(220px, 1fr))', padding: '24px 4px 32px' }}>
  {[
    ['flat', 'Resting cards and rows. The lowest lift the system has that is still not flat.'],
    ['raised', 'A card that has lifted, for hoverable surfaces at rest.'],
    ['raised-hover', 'The hover partner of raised. Only ever paired with it.'],
    ['menu', 'Dropdowns and popovers. Above the page, not covering it.'],
    ['tray', 'The bottom selection tray. Two-layer shadow: spread plus contact.'],
    ['overlay-dark', 'Content over the 3D viewport, where the backdrop is unpredictable.'],
    ['card-warm', 'Warm-tinted card shadow. Brown-based, not black, because Envision is a warm palette.'],
  ].map(([name, use]) => (
    <div key={name}>
      <div
        style={{
          height: 84,
          borderRadius: 'var(--envision-t2-border-radius-card)',
          background: 'var(--envision-t2-color-background-surface-default)',
          boxShadow: `var(--envision-t2-elevation-${name})`,
          marginBottom: 12,
        }}
      />
      <div style={{ fontWeight: 600, fontSize: 14 }}>{name}</div>
      <div style={{ fontSize: 13, opacity: 0.75, lineHeight: 1.5 }}>{use}</div>
      <code style={{ fontSize: 11 }}>--envision-t2-elevation-{name}</code>
    </div>
  ))}
</div>

### Why `card-warm` exists
Every other shadow is black at low alpha. `card-warm` is **brown** (`rgba(118, 93, 73, .12)`),
because a neutral black shadow on Envision's warm surfaces reads as gray and dirty. It is the one
place the shadow ramp acknowledges the palette. Use it on warm surfaces; use `raised` on white.

### Rules
- **Elevation is not a z-index.** Layering order is the `z-index` foundation; elevation is only the
  shadow. An element can be visually flat and still on top.
- **Never author a shadow inline.** If no role fits, that is a system gap to raise, not a value to
  invent.
- Elevation lives in Figma as **effect styles**, which is why it is absent from the variable
  export and is snapshotted separately in the token pipeline.
